#!/usr/bin/env bash
set -e

APP_DIR=/opt/visionotg
export ORT_DYLIB_PATH="$APP_DIR/lib/libonnxruntime.so"
export LD_LIBRARY_PATH="$APP_DIR/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
exec "$APP_DIR/votg" "$@"